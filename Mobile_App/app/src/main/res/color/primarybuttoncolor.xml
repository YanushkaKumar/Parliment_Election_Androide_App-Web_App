<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:drawable="@color/purple" android:state_pressed="true"/>
    <!-- Focused state -->
    <item android:drawable="@color/colorPrimaryLight" android:state_focused="true"/>
    <!-- Default normal state -->
    <item android:drawable="@color/colorPrimary"/>
</selector>
